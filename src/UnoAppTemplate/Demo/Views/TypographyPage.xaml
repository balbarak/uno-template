<Page
    x:Class="UnoAppTemplate.Views.TypographyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UnoAppTemplate.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource Header-Height}" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <local:PageHeader Header="Welcome s" />

        <Grid Grid.Row="1" Background="{ThemeResource SurfaceContainerLowColor}">

            <ScrollViewer>
                <StackPanel Margin="{StaticResource Container-Thickness}" Spacing="16">
                    <Grid Style="{ThemeResource Card}">
                        <Grid Style="{StaticResource Card-Content}">
                            <StackPanel>
                                <TextBlock Style="{ThemeResource Card-Title}" Text="Headline Typography" />
                                <TextBlock Style="{ThemeResource Card-SubTitle}" Text="Headlines text styles" />
                                <TextBlock Style="{StaticResource Display-Large}" Text="Display Large" />
                                <TextBlock Style="{StaticResource Display-Medium}" Text="Display Medium" />
                                <TextBlock Style="{StaticResource Display-Small}" Text="Display Small" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                    <Grid Style="{ThemeResource Card}">
                        <Grid Style="{StaticResource Card-Content}">
                            <StackPanel>
                                <TextBlock Style="{ThemeResource Card-Title}" Text="Headline Typography" />
                                <TextBlock Style="{ThemeResource Card-SubTitle}" Text="Headlines text styles" />
                                <TextBlock Style="{StaticResource Headline-Large}" Text="Headline Large" />
                                <TextBlock Style="{StaticResource Headline-Medium}" Text="Headline Medium" />
                                <TextBlock Style="{StaticResource Headline-Small}" Text="Headline Small" />
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Grid Style="{ThemeResource Card}">
                        <Grid Style="{StaticResource Card-Content}">
                            <StackPanel>
                                <TextBlock Style="{ThemeResource Card-Title}" Text="Title Typography" />
                                <TextBlock Style="{ThemeResource Card-SubTitle}" Text="Titles text styles" />
                                <TextBlock Style="{StaticResource Title-Large}" Text="Title Large - Roboto Regular" />
                                <TextBlock Style="{StaticResource Title-Medium}" Text="Title Medium - Roboto Regular" />
                                <TextBlock Style="{StaticResource Title-Small}" Text="Title Small - Roboto Regular" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                    <Grid Style="{ThemeResource Card}">
                        <Grid Style="{StaticResource Card-Content}">
                            <StackPanel>
                                <TextBlock Style="{ThemeResource Card-Title}" Text="Body Typography" />
                                <TextBlock Style="{ThemeResource Card-SubTitle}" Text="Body text styles" />
                                <TextBlock Style="{StaticResource Body-Large}" Text="Body Large - Roboto Regular" />
                                <TextBlock Style="{StaticResource Body-Medium}" Text="Body Medium - Roboto Regular" />
                                <TextBlock Style="{StaticResource Body-Small}" Text="Body Small - Roboto Regular" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>


</Page>
