<UserControl
    x:Class="UnoAppTemplate.Controls.TextFormGroup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UnoAppTemplate.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Focusable="True"
    mc:Ignorable="d">

    <Grid
        x:Name="PART_Root"
        BorderBrush="{ThemeResource OutlineColor}"
        BorderThickness="1"
        CornerRadius="4"
        Focusable="True">
        <TextBox
            x:Name="PART_InputElement"
            Padding="16"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            IsTabStop="True"
            TextChanged="OnTextChanged">
            <TextBox.Template>
                <ControlTemplate TargetType="TextBox">
                    <Grid x:Name="PART_TextContainer" Background="{TemplateBinding Background}">
                        <ScrollViewer
                            x:Name="ContentElement"
                            Padding="{TemplateBinding Padding}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            HorizontalContentAlignment="Stretch"
                            AutomationProperties.AccessibilityView="Raw"
                            HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                            HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                            IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                            IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
                            IsTabStop="False"
                            IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
                            VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                            VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                            ZoomMode="Disabled">
                            <ScrollViewer.RenderTransform>
                                <CompositeTransform x:Name="ContentTransform" />
                            </ScrollViewer.RenderTransform>
                        </ScrollViewer>


                    </Grid>
                </ControlTemplate>
            </TextBox.Template>

        </TextBox>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="Focused">
                    <Storyboard>
                        <!--<DoubleAnimation
                            Storyboard.TargetName="PART_Root"
                            Storyboard.TargetProperty="CornerRadius"
                            From="0"
                            To="5"
                            Duration="00:00:0.25">
                            <DoubleAnimation.EasingFunction>
                                <CubicEase EasingMode="EaseInOut" />
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>-->

                        <!--<ObjectAnimationUsingKeyFrames
                            Storyboard.TargetName="PART_Root"
                            Storyboard.TargetProperty="BorderBrush"
                            Duration="00:00:00.25">
                            <DiscreteObjectKeyFrame KeyTime="00:00:00.05" Value="Green" />
                            <DiscreteObjectKeyFrame KeyTime="00:00:00.10" Value="Yellow" />
                            <DiscreteObjectKeyFrame KeyTime="00:00:00.15" Value="Blue" />
                            <DiscreteObjectKeyFrame KeyTime="00:00:00.25" Value="Red" />
                        </ObjectAnimationUsingKeyFrames>-->

                    </Storyboard>
                    <VisualState.Setters>
                        <Setter Target="PART_Root.BorderBrush" Value="{ThemeResource PrimaryColor}" />
                        <Setter Target="PART_Root.BorderThickness" Value="2" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
